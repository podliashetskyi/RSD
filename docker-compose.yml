services:
  web:
    build:
      context: .
      dockerfile: RSD.Web/Dockerfile
    container_name: rsd-website
    ports:
      - "8082:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
    restart: unless-stopped
