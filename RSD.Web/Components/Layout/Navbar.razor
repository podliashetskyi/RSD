<nav class="bg-white px-4 lg:px-section-x py-4 relative">
    <div class="max-w-screen-xl mx-auto flex items-center gap-8">
        <a href="/" class="flex items-center shrink-0 lg:flex-1" aria-label="RSD home">
            <img src="images/logo.svg" alt="RSD" width="65" height="32" class="h-8 w-auto" />
        </a>

        <div class="hidden lg:flex lg:items-center lg:justify-center lg:gap-2">
            <NavLink href="/" Match="NavLinkMatch.All" class="nav-link">Home</NavLink>
            <NavLink href="/services" class="nav-link">Services</NavLink>
            <NavLink href="/products" class="nav-link">Products</NavLink>
            <NavLink href="/cases" class="nav-link">Cases</NavLink>
            <NavLink href="/about" class="nav-link inline-flex items-center gap-1">
                About Us
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                </svg>
            </NavLink>
            <NavLink href="/contact" class="nav-link">Contact</NavLink>
            <NavLink href="/blog" class="nav-link">Blog</NavLink>
        </div>

        <div class="hidden lg:flex lg:flex-1 lg:justify-end">
            <Button Href="/contact" Variant="ButtonVariant.Primary" Size="ButtonSize.Lg">Start Project</Button>
        </div>

        <div class="flex-1 lg:hidden"></div>

        <button data-collapse-toggle="mobile-menu"
                type="button"
                class="inline-flex items-center justify-center shrink-0 w-10 h-10 text-ink-soft rounded-lg lg:hidden hover:bg-line-subtle group"
                aria-controls="mobile-menu"
                aria-expanded="false"
                aria-label="Toggle navigation menu">
            <svg class="w-6 h-6 group-aria-expanded:hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
            </svg>
            <svg class="w-6 h-6 hidden group-aria-expanded:block" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
            </svg>
        </button>
    </div>

    <div id="mobile-menu"
         class="hidden lg:!hidden flex flex-col gap-2 absolute top-full left-0 right-0 bg-white border-b border-line z-50 p-4 shadow-lg">
        <NavLink href="/" Match="NavLinkMatch.All" class="nav-link">Home</NavLink>
        <NavLink href="/services" class="nav-link">Services</NavLink>
        <NavLink href="/products" class="nav-link">Products</NavLink>
        <NavLink href="/cases" class="nav-link">Cases</NavLink>
        <NavLink href="/about" class="nav-link inline-flex items-center gap-1">
            About Us
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
            </svg>
        </NavLink>
        <NavLink href="/contact" class="nav-link">Contact</NavLink>
        <NavLink href="/blog" class="nav-link">Blog</NavLink>
        <div class="pt-2">
            <Button Href="/contact" Variant="ButtonVariant.Primary" Size="ButtonSize.Lg" AdditionalClasses="w-full">Start Project</Button>
        </div>
    </div>
</nav>
